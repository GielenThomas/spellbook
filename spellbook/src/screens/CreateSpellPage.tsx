import React from "react";
import { Text, View } from "react-native";
export const CreateSpellPage = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: "white",
      }}
    >
      <Text style={{ color: "black" }}>Create Spell</Text>
    </View>
  );
};
